# App overlays

This folder contains runtime+lab overlays.

- `observability-k3d/`
- `networking-kind/`
- `crossplane-k3s/`

Each overlay may define:

- Kustomize overlays for YAML apps
- Helm values overrides for chart-based apps
